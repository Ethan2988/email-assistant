# Email Assistant

<div align="center">

**AI-Powered Intelligent Email Automation System**

[![Python Version](https://img.shields.io/badge/python-3.13+-blue.svg)](https://www.python.org/downloads/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Code style: black](https://img.shields.io/badge/code%20style-black-000000.svg)](https://github.com/psf/black)

基于 LangGraph + DeepSeek LLM 的智能邮件自动化系统

[功能特性](#功能特性) • [快速开始](#快速开始) • [架构设计](#架构设计) • [文档](#文档)

</div>

---

## 项目简介

**Email Assistant** 是一个基于 AI 的智能邮件自动化系统，通过集成大语言模型（LLM）实现邮件的智能处理、自动回复和任务管理。系统采用 LangGraph 构建状态机工作流，使用 LangChain 工具生态，提供可扩展的邮件自动化能力。

### 核心亮点

- 🤖 **AI 智能决策**：基于 DeepSeek LLM 的智能邮件分析和自动回复
- 📧 **实时邮件监听**：支持 IMAP IDLE 实时推送和轮询模式
- 🔄 **工作流编排**：LangGraph 状态机实现灵活的邮件处理流程
- 🛠️ **可扩展工具系统**：基于 LangChain 的插件化工具架构
- 👥 **联系人管理**：完整的联系人管理系统，支持模糊搜索和批量操作
- ⏰ **定时任务调度**：支持多种类型的定时任务管理
- 💾 **统一数据存储**：SQLite 数据库统一管理所有数据表

---

## 功能特性

### 1. 智能邮件处理
- 自动监听收件箱新邮件
- AI 分析邮件内容并生成智能回复
- 自动识别发件人并选择性处理
- 防止重复处理和回复自己发送的邮件

### 2. 邮件监听与发送
- 支持 IMAP IDLE 实时推送（零延迟）
- 自动回退到轮询模式（兼容所有邮件服务器）
- SMTP 邮件发送，支持 HTML 和附件
- QQ 邮箱、163 邮箱等主流邮箱支持

### 3. 联系人管理
- 联系人 CRUD 操作（增、删、改、查）
- 邮箱格式验证和唯一性检查
- 模糊搜索（支持 ID、姓名、邮箱）
- 批量添加和删除功能
- SQLAlchemy ORM 管理数据

### 4. 定时任务调度
- 每日定时任务（cron 表达式）
- 一次性任务（指定时间执行）
- 间隔执行任务
- 任务持久化到 SQLite
- APScheduler 强大调度能力

### 5. 工具扩展系统
- 基于 LangChain 的工具框架
- 支持动态添加自定义工具
- 工具调用链和多轮对话
- 统一的错误处理和日志记录

---

## 快速开始

### 环境要求

- Python 3.13+
- QQ 邮箱或其他支持 IMAP/SMTP 的邮箱
- DeepSeek API Key

### 安装

```bash

cd email-assistant

# 使用 uv 安装（推荐）
uv sync

# 或使用 pip 安装
pip install -e .
```

### 配置

1. 复制配置文件模板：

```bash
cp config.yaml.example config.yaml
```

2. 编辑 `config.yaml` 填写配置信息：

```yaml
llm:
  model: "deepseek-chat"
  api_key: "your-deepseek-api-key"  # DeepSeek API Key

email:
  email_sender: "your-email@qq.com"     # 助理邮箱
  auth_code: "your-auth-code"           # QQ 邮箱授权码（非登录密码）
  smtp_server: "smtp.qq.com"
  smtp_port: 465
  imap_server: "imap.qq.com"
  imap_port: 993

master:
  master_email: "master@example.com"    # 主人邮箱
  master_name: "Ethan"                  # 主人姓名
```

### 获取 QQ 邮箱授权码

1. 登录 QQ 邮箱网页版
2. 进入「设置」→「账户」
3. 开启 IMAP/SMTP 服务
4. 生成授权码（16位字符）

### 获取 DeepSeek API Key

1. 访问 [DeepSeek 开放平台](https://platform.deepseek.com/)
2. 注册并登录
3. 在 API Keys 页面创建新的 API Key

### 运行

```bash
# 开发环境运行
python -m email_assistant.main

# 使用命令行工具
email-assistant

# 后台运行
nohup python -m email_assistant.main > logs/app.log 2>&1 &
```

---

## 架构设计

### 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      Email Assistant System                   │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐      ┌──────────────┐      ┌───────────┐  │
│  │ EmailListener│─────>│  EmailAgent  │─────>│ Workflow  │  │
│  │  (IMAP IDLE) │      │  (LLM Core)  │      │  Nodes    │  │
│  └──────────────┘      └──────────────┘      └───────────┘  │
│         │                       │                     │      │
│         v                       v                     v      │
│  ┌──────────────┐      ┌──────────────┐      ┌───────────┐  │
│  │ EmailClient  │      │  Tool System │      │  Services │  │
│  │   (IMAP)     │      │  (LangChain) │      │   Layer   │  │
│  └──────────────┘      └──────────────┘      └───────────┘  │
│         │                       │                     │      │
│         v                       v                     v      │
│  ┌──────────────┐      ┌──────────────┐      ┌───────────┐  │
│  │  Mail Server │      │  LLM APIs    │      │   SQLite  │  │
│  │   (QQ Mail)  │      │ (DeepSeek)   │      │ Database  │  │
│  └──────────────┘      └──────────────┘      └───────────┘  │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 核心组件

#### 1. EmailAgent（邮件代理）
系统的核心决策中心，集成 DeepSeek LLM 进行智能处理，编排 LangGraph 工作流。

#### 2. WorkflowNodes（工作流节点）
- `process_email`：分析邮件内容，调用 LLM 生成响应
- `tools`：执行工具调用
- `send_email`：发送邮件
- `mark_as_replied`：标记已处理

#### 3. 邮件服务层
- `EmailListener`：IMAP 邮件监听服务
- `SendEmailService`：SMTP 邮件发送服务
- `ContactService`：联系人业务逻辑
- `SchedulerService`：定时任务调度

#### 4. 工具系统（LangChain Tools）
- `EmailTool`：邮件发送工具
- `SchedulerTaskTool`：定时任务管理工具
- `ContactTool`：联系人管理工具

#### 5. 数据模型
- `Contact`：联系人模型（SQLAlchemy ORM）
- `SchedulerTaskModel`：定时任务模型
- `DatabaseManager`：统一数据库管理器

---

## 项目结构

```
email_assistant/
├── src/email_assistant/          # 主应用代码
│   ├── agents/                   # AI 代理和工作流
│   ├── service/                  # 服务层（邮件、调度、联系人）
│   ├── tools/                    # LangChain 工具
│   ├── models/                   # 数据模型
│   ├── config/                   # 配置管理
│   ├── utils/                    # 工具函数
│   ├── system_init.py           # 系统初始化
│   └── main.py                  # 应用入口
│
├── tests/                        # 测试代码
├── examples/                     # 示例和文档
├── data/                         # 数据目录
│   └── data.db                   # 统一数据库文件
│
├── config.yaml                   # 系统配置文件
├── pyproject.toml               # 项目依赖
├── README.md                     # 项目说明
├── CLAUDE.md                     # 架构文档
└── LICENSE                       # MIT 协议
```

---

## 使用示例

### 基本邮件处理

系统会自动监听来自 Master 的邮件并处理：

```
发件人: master@example.com
主题: 帮我查询明天天气
内容: 北京明天天气怎么样

↓ 系统自动分析并回复

收件人: master@example.com
主题: Re: 帮我查询明天天气
内容: 根据天气预报，北京明天晴转多云，气温 5-15℃...
```

### 联系人管理

```python
from email_assistant.service.contact_service import ContactService

# 初始化服务
contact_service = ContactService()

# 添加联系人
result = contact_service.add_contact(
    name="张三",
    email="zhangsan@example.com",
    remark="大学同学"
)

# 搜索联系人
result = contact_service.search_contacts(keyword="张三")

# 批量添加
contacts = [
    {"name": "李四", "email": "lisi@example.com"},
    {"name": "王五", "email": "wangwu@example.com"}
]
result = contact_service.batch_add_contacts(contacts)
```

### 定时任务

```python
from email_assistant.service.scheduler_service import SchedulerService

# 初始化服务
scheduler_service = SchedulerService()

# 添加每日任务
scheduler_service.add_daily_task(
    content="每天早上9点发送天气预报",
    schedule_time="09:00"
)

# 添加一次性任务
scheduler_service.add_one_time_task(
    content="明天下午3点提醒开会",
    schedule_time="2025-01-02 15:00:00"
)

# 查询所有任务
tasks = scheduler_service.get_all_tasks()
```

---

## 测试

```bash
# 运行所有测试
pytest

# 运行特定测试文件
pytest tests/test_service/test_contact_service.py

# 带覆盖率报告
pytest --cov=src/email_assistant --cov-report=html

# 运行测试并查看详细输出
pytest -v -s
```

---

## 开发指南

### 添加新工具

1. 在 `src/email_assistant/tools/` 创建新工具文件
2. 继承 `BaseTool`
3. 实现 `_run` 方法
4. 在 `email_agent.py` 中注册工具

示例：

```python
from langchain_core.tools import BaseTool

class MyCustomTool(BaseTool):
    name = "my_custom_tool"
    description = "工具描述"

    def _run(self, query: str) -> str:
        # 工具逻辑
        return result
```

### 添加新的工作流节点

1. 在 `agent_nodes.py` 中添加节点方法
2. 在 `email_agent.py` 中注册节点
3. 添加边和路由逻辑

---

## 部署

### 生产环境建议

1. **使用进程管理工具**
   ```bash
   # 使用 systemd
   sudo systemctl start email-assistant

   # 使用 supervisor
   supervisord -c supervisor.conf
   ```

2. **Docker 容器化部署**
   ```bash
   docker build -t email-assistant .
   docker run -d --name email-assistant email-assistant
   ```

3. **日志管理**
   - 配置日志轮转（logrotate）
   - 使用集中式日志管理（ELK/Loki）

4. **监控告警**
   - 配置健康检查接口
   - 集成 Prometheus + Grafana
   - 邮件/短信告警

---

## 常见问题

### Q1: 邮件发送失败怎么办？
- 检查 SMTP 配置（服务器、端口）
- 确认使用授权码而非登录密码
- 检查网络连接
- 使用 `test_email_connection` 工具诊断

### Q2: 如何处理大量邮件？
- 系统采用单线程顺序处理
- 可以调整轮询间隔
- 建议使用邮件过滤规则减少不必要处理

### Q3: LLM API 调用失败如何处理？
- 系统会捕获异常并记录错误
- 可以添加重试机制
- 考虑配置备用 LLM API

### Q4: 如何添加多个 Master 用户？
- 修改 `process_email` 节点逻辑
- 支持 Master 列表配置
- 调整发件人判断逻辑

---

## 技术栈

### 核心框架
- **Python 3.13+**：主要开发语言
- **LangGraph**：工作流编排和状态机
- **LangChain**：LLM 集成和工具系统
- **DeepSeek**：主要 LLM 提供商

### 邮件处理
- **imaplib2**：IMAP 协议处理（支持 IDLE）
- **smtplib**：SMTP 邮件发送
- **email-validator**：邮箱地址验证

### 任务调度
- **APScheduler**：强大的 Python 任务调度库

### 数据持久化
- **SQLite**：轻量级数据库
- **SQLAlchemy 2.0+**：ORM 框架

### 配置管理
- **PyYAML**：YAML 配置文件解析
- **python-dotenv**：环境变量管理

### 测试框架
- **pytest**：单元测试和集成测试
- **pytest-mock**：Mock 工具
- **pytest-cov**：代码覆盖率

---

## 路线图

### v0.3.0（计划中）
- [ ] 支持多 Master 用户
- [ ] 邮件分类和标签管理
- [ ] Web 管理界面
- [ ] 更多 LLM 模型支持

### v0.2.0（当前版本）
- [x] 联系人管理系统
- [x] 统一数据库架构
- [x] 批量操作支持
- [x] 模糊搜索功能

### v0.1.0
- [x] 初始版本发布
- [x] LangGraph 工作流系统
- [x] DeepSeek LLM 集成
- [x] 邮件监听和自动回复
- [x] 定时任务管理

---

## 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: 添加某个功能'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码规范
- 遵循 PEP 8
- 使用类型注解
- 添加 docstring
- 编写单元测试

### 提交规范
```
feat: 添加新功能
fix: 修复 Bug
docs: 更新文档
test: 添加测试
refactor: 代码重构
```

---

## 许可证

本项目采用 [MIT License](LICENSE) 开源协议。

---

## 联系方式

- **维护者**: Ethan
- **邮箱**: ethan2088@foxmail.com
- **GitHub**: https://github.com/Ethan2988/email-assistant

---

## 致谢

- [LangGraph](https://github.com/langchain-ai/langgraph) - 强大的工作流编排框架
- [LangChain](https://python.langchain.com/) - LLM 应用开发框架
- [DeepSeek](https://platform.deepseek.com/) - 提供 LLM API 支持
- [APScheduler](https://apscheduler.readthedocs.io/) - 任务调度库

---

<div align="center">

**如果这个项目对你有帮助，请给一个 ⭐️ Star！**

Made with ❤️ by Ethan

</div>
